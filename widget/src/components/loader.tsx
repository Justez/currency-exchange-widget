import React from 'react';
import CircularProgress, { CircularProgressProps } from '@material-ui/core/CircularProgress';

const FlipChip = (props: CircularProgressProps) => (
    <CircularProgress {...props} />
);

export default FlipChip;